MVP:
    implement config presets for common tools that can be extended in user config
    implement init command (generates an initial config w/ suggested presets)
    implement fix command
    non-zero exit code when results
    prune "file type" logic from indexer and processor
    change Command.Template yaml field to "command"

    add app version, date, and revision vars
    setup go-releaser

    github repo setup

V1:
    implement regexp parser
    implement sarif parser
    implement diff parser
    implement sarif printer

V?
    editor config support
